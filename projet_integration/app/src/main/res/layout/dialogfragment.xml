<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="24dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:layout_marginTop="80dp"
    android:layout_marginBottom="40dp"
    android:elevation="12dp"
    android:clipToPadding="false"
    android:background="@drawable/dialog_background"
    android:layout_marginStart="20dp"
    android:layout_marginEnd="20dp">

    <!-- Icône -->
    <ImageView
        android:id="@+id/dialog_icon"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_gravity="center"
        android:src="@drawable/baseline_priority_high_24"
        android:tint="@color/black"
        android:layout_marginTop="20dp"/>

    <!-- Titre -->
    <TextView
        android:id="@+id/dialog_title"
        android:text="Réclamation"
        android:textSize="26sp"
        android:textColor="#333333"
        android:layout_marginTop="20dp"
        android:layout_gravity="center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:typeface="sans"
        android:letterSpacing="0.05" />

    <!-- Champ pour soumettre la réclamation -->
    <EditText
        android:id="@+id/dialog_message_input"
        android:hint="Votre réclamation ici..."
        android:textSize="16sp"
        android:padding="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="top|start"
        android:inputType="textMultiLine"
        android:minHeight="120dp"
        android:maxLines="5"
        android:layout_marginTop="20dp"
        android:textColor="#333333"
        android:background="@drawable/edittext_background"
        android:textColorHint="#B0B0B0"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:importantForAccessibility="yes"
        android:contentDescription="Champ de texte pour la réclamation" />

    <!-- Boutons -->
    <LinearLayout
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp">

        <!-- Bouton OK (utilise MaterialButton pour cornerRadius) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/dialog_ok"
            android:text="Envoyer"
            android:backgroundTint="@color/buttonGreen"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:padding="14dp"
            android:textSize="12sp"
            android:layout_gravity="center_vertical"
            android:layout_marginEnd="12dp"
            android:textColor="@android:color/white"
            android:elevation="8dp"
            android:transitionName="envoyer_button"
            android:scaleX="1.0"
            android:scaleY="1.0"
            android:layout_weight="1" />


        <com.google.android.material.button.MaterialButton
            android:id="@+id/dialog_cancel"
            android:text="Annuler"
            android:backgroundTint="@color/buttonRed"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:padding="14dp"
            android:textSize="12sp"
            android:layout_gravity="center_vertical"
            android:textColor="@android:color/white"
            android:elevation="8dp"
            android:transitionName="annuler_button"
            android:scaleX="1.0"
            android:scaleY="1.0"
            android:layout_weight="1" /> <!-- Utilisation de layout_weight pour partager l'espace -->

    </LinearLayout>
</LinearLayout>